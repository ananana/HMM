function [ a, b, pi ] = random_model (obs_size, nr_states)

a = [];
b = [];
pi = rand_prob_vect(nr_states);
for i=1:nr_states
	b = [b; rand_prob_vect(obs_size)];
	a = [a; rand_prob_vect(nr_states)];
end

end
